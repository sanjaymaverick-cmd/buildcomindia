# Buildcom India — granite website

Static site for **buildcomindia.com**.

Full package with optimized yard photos: download `buildcomindia-website.zip` from the project folder and upload images into `/images`.

Pages: `index.html`, `catalogue.html`, `about.html`, `contact.html`.

Before publish: set WhatsApp number in `contact.html` (`data-whatsapp`).

GoDaddy: upload extracted folder to `public_html` so `index.html` is at the root. Or connect this repo to Vercel and point A record `@` to `76.76.21.21`.
